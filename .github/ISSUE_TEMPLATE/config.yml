# Discussions is not enabled on this repo, so there is deliberately no contact
# link here — a dead link in the issue chooser is worse than none. Blank issues
# stay enabled so anything the templates do not cover still has a home.
blank_issues_enabled: true
